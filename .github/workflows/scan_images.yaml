name: Publish

on:
  workflow_dispatch:

jobs:

  on-push:
    name: Get rocks modified and build-scan-test-publish them
    uses: canonical/charmed-kubeflow-workflows/.github/workflows/get-published-images-scan-and-report.yaml@KF-6331-add-scan-reusable-workflow
    secrets:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
    with:
      report-vulnerabilities: true
      severity: "HIGH,CRITICAL"
